# Git-конфликт

Конфликт возник в файле `README.md` при объединении ветки `feature` с веткой `main`.

В обеих ветках была изменена одна и та же строка.

## Как был исправлен

Конфликт был исправлен вручную. Я оставил вариант описания:

> Проект предназначен для организации кинопроката.

После исправления был создан отдельный коммит:

`Разрешен конфликт в README`